export default function ServicesDataModel(id, serviceName, region, dataClassification, platform, product, action) {
    return { id, serviceName, region, dataClassification, platform, product, action }
}
