export default function DataModel(appCode, requestId, useCaseSummary, services, requstedBy, submittedOn, status) {
  return {appCode, requestId, useCaseSummary, services, requstedBy, submittedOn, status}
}
